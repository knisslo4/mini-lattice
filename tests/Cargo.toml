[package]
name = "tests"
version = "0.1.0"
edition = "2024"

[dependencies]
data_model = { path = "../data_model" }
serde_json = "1.0"
uuid = { version = "1", features = ["serde", "v4"] }
chrono = { version = "0.4", features = ["serde"] }